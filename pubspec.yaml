name: meulog_shared
description: "Componentes compartilhados para os projetos Flutter do meulog"
version: 0.0.1
homepage:

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  timezone:
  intl:
  intl_translation:

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true